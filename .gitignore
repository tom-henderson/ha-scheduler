# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Node / card build
node_modules/
*.tsbuildinfo

# Built card bundle — generated by `npm run build`, not committed. CI builds it
# and ships it in the release zip (HACS installs via zip_release). Build locally
# before running the Playwright smoke tests or a manual install.
custom_components/daily_schedule/frontend/daily-schedule-card.js

# Card test artifacts
card/test-results/
card/playwright-report/

# Editor / OS
.DS_Store
.idea/
.vscode/
